ktor {
  deployment {
    port = 80
    sslPort = 443
    autoreload = true
    watch = []
  }

  application {
    modules = [processm.services.StaticContentKt.main]
  }

  security {
    ssl {
      keyStore = null  // set default value to null; main() will generate self-signed certificate if not present
      keyAlias = "ssl"
      keyStorePassword = ""
      privateKeyPassword = ""
    }
  }
}
