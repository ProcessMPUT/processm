package processm.core.models.metadata

/**
 * An interface representing value of some metadata. By itself does not contain anything and serves as a marker rather than a contract.
 */
interface MetadataValue {
}